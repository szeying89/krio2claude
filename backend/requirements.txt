# Runtime dependencies for the Ground-Truth Threat Modelling Platform backend.
#
# This mirrors [project.dependencies] in pyproject.toml, which remains the
# source of truth (used by `uv pip install -e ".[dev]"` in CI and the
# README's quickstart). This file exists as a plain-pip alternative for
# environments without uv:
#
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# For development (tests, lint, type-checking), also install requirements-dev.txt.

fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0
alembic>=1.13
pydantic>=2.9
pydantic-settings>=2.6
sse-starlette>=2.1
aiosqlite>=0.20
python-multipart>=0.0.12
pypdf>=5.0
python-docx>=1.1
pyyaml>=6.0
httpx>=0.27
openpyxl>=3.1
scikit-learn>=1.5
numpy>=2.0
networkx>=3.3
jsonschema>=4.23
