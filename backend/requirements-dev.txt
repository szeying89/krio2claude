# Development dependencies (tests, lint, type-checking), on top of the
# runtime requirements. Mirrors [project.optional-dependencies].dev in
# pyproject.toml.
#
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest>=8.3
pytest-asyncio>=0.24
ruff>=0.7
mypy>=1.13
types-PyYAML>=6.0
